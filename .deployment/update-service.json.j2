{
    "cluster": "{{ ENVIRONMENT }}-trino-service",
    "service": "{{ ENVIRONMENT }}-trino-service",
    "desiredCount": {{ TRINO_DESIRED_COUNT }},
    "taskDefinition": "{{ ENVIRONMENT }}-trino-service",
    "healthCheckGracePeriodSeconds": 120
}